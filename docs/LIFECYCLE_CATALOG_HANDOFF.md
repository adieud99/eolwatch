# EOL 카탈로그 검증 인계 — 2026-09-16

사용자가 오늘 작업 종료를 요청하여 추가 실행을 멈췄다. 개발 기능은 배포됐지만 **라이브 적용·상속 검증은 아직 완료하지 않았다.**

## 완료된 확인

- 신규 backend 집중 테스트 38개, UI 테스트 9개 통과. 통합 전체 검사 결과는 루트 검증 기록을 따른다.
- 공개 API 클라이언트의 전체 제품 475개·PostgreSQL 지원 주기 29개 파싱 확인: [공개 응답 증거](../reports/lifecycle-catalog-public-check.json).
- 웹 `http://127.0.0.1:18080`에서 공개 제품 목록 갱신 성공. 저장된 목록의 검색·출처·수집 시각·원본 SHA 화면 확인.
- 브라우저/API 오류는 관찰되지 않았다.

## 이번 실행에서 생성한 검증 데이터

| 종류 | 값 |
| --- | --- |
| 자산 | #4, `VERIFY-UI-20260915144735826`, `monitored=false` |
| 새 제품 | #1381, `VERIFY EOL PostgreSQL 16 88d74686`, 버전 16.4 |
| 새 SBOM | #23, CycloneDX 1.6 |
| 새 구성요소 | #5537, 제품 #1381 연결 |
| 최초 지원 상태 | `UNKNOWN`, 종료일·근거 URL 없음 |
| 공개 일정 적용 | **미실행** |

위 제품과 구성요소는 공개 PostgreSQL 16 지원 주기를 검증하기 위한 명시적 모형 데이터다. 실제 설치된 PostgreSQL로 표현하지 않는다. 기존 자산 설정·기존 제품 날짜·조치 상태를 변경하는 요청은 보내지 않았다.

## 중단 지점

브라우저 스크립트가 `PostgreSQL · postgresql` 옵션을 찾았지만, 닫힌 네이티브 `<select>`의 `<option>`에 `visible` 대기를 사용하여 60초 뒤 종료됐다. 기록에는 해당 옵션이 DOM에 존재하는 것으로 나온다. 이것은 검증 스크립트의 대기 조건 문제이며 앱/API 실패로 확인된 사항은 아니다.

- 시작: 2026-09-15 16:00:27 UTC / 2026-09-16 01:00:27 KST
- 종료: 2026-09-15 16:01:31 UTC / 2026-09-16 01:01:31 KST
- 스크립트는 종료됐고 재실행하지 않았다.
- 실제 변경 요청: 로그인, 새 소프트웨어 등록, 새 SBOM 반입, 공개 제품 목록 갱신.
- 개별 PostgreSQL 일정 조회·미리보기·적용 요청은 보내지 않았다.

[실행 JSON](../reports/lifecycle-catalog-live-verification.json) · [중단 당시 실제 화면](../reports/lifecycle-catalog-failure.png)

## 다음 세션에서 이어갈 일

1. `/private/tmp/eolwatch-browser-check/lifecycle-catalog-live.mjs`의 옵션 대기를 `attached`로 변경하거나 `selectOption`을 직접 사용한다. 공개 제품 옵션과 지원 주기 옵션 양쪽을 확인한다.
2. 이미 생성한 **제품 #1381 / SBOM #23 / 구성요소 #5537**을 재사용하도록 준비 단계를 조정한다. 전체 스크립트를 그대로 다시 실행하면 새 검증 제품이 추가된다.
3. 공개 제품 `postgresql`의 상세를 조회하고 지원 주기 `16`을 선택한다.
4. 표준 지원 종료일 `2028-11-09`의 미리보기와 기존 값 차이를 확인한 뒤, 검증 제품에만 적용한다.
5. 적용 이력의 원본 SHA·출처·제공처 생성 시각·수집 시각·적용 시각과 구성요소의 제품 날짜 상속을 확인한다.
6. 실행 전 존재한 제품 날짜와 자산 설정이 유지됐는지 최종 비교한다. 이번 실행은 그 최종 비교 단계에 도달하지 않았으므로 완료로 표시하지 않는다.

공개 제품 목록 SHA-256: `1e768fdefc82f53b1354a2196a6c35a14636c612765d29ea04c4b01220ba87c0`.
